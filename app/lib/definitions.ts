export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type Expense = {
  id: string;
  email: string;
  category: string;
  typeOfExp: string;
  amount: number;
  date: string;
};
